node_modules/
.env
.env.local

# Playwright capture output — regenerate with `node shot.js` / `node orbit.js`
*.png

# Your own listening history + embedded album art. Never publish:
# GitHub Pages serves the branch tree publicly.
deadwax-data.js

# Scratch: agent walkthrough artifacts and staging for deletion.
_uxwalk/
_to_delete/

# Verification scratch scripts, not part of the build.
baseline-probe.js
about-shot.js
about-scroll.js
vol-test.js
about*.png
baseline*.png
v-*.png
